<?xml version="1.0" ?>
<odoo>

  <!-- Mail template are declared in a NOUPDATE block so users can freely customize/delete them -->
  <data>
    <!--Email template -->
    <record id="email_template_edi_invoice_odoofact" model="mail.template">
      <field name="name">Invoice - Send by Email</field>
      <field name="model_id" ref="account.model_account_invoice"/>
      <field name="email_from">${(object.user_id.email and '%s &lt;%s&gt;' % (object.user_id.name, object.user_id.email) or '')|safe}</field>
      <field name="subject">${object.company_id.name} Invoice (Ref ${object.number or 'n/a'})</field>
      <field name="partner_to">${object.partner_id.id}</field>
      <field name="body_html"><![CDATA[
        <table border="0" cellpadding="0" cellspacing="0" style="padding-top: 16px; background-color: #F1F1F1; font-family:Verdana, Arial,sans-serif; color: #454748; width: 100%; border-collapse:separate;">
          <tr>
            <td align="center">
              % set company = ctx.get('company', user.company_id)
              <table border="0" cellpadding="0" cellspacing="0" width="590" style="padding: 24px; background-color: white; color: #454748; border-collapse:separate;">
                <tbody>
                  <!-- HEADER -->
                  <tr>
                    <td align="center" style="min-width: 590px;">
                      <table border="0" cellpadding="0" cellspacing="0" width="100%" style="background-color: white; padding: 0; border-collapse:separate;">
                        <tr>
                          <td valign="middle">
                            <span style="font-size: 10px;">Su comprobante de pago electrónico</span><br/>
                            <span style="font-size: 20px; font-weight: bold;">
                              ${object.number}
                            </span>
                          </td>
                          <td valign="middle" align="right">
                            <img src="/logo.png?company=${object.company_id.id}" style="padding: 0px; margin: 0px; height: 48px;" alt="${object.company_id.name}"/>
                          </td>
                        </tr>
                        <tr>
                          <td colspan="2" style="text-align:center;">
                            <hr width="100%" style="background-color:rgb(204,204,204);border:medium none;clear:both;display:block;font-size:0px;min-height:1px;line-height:0; margin:4px 0px 32px 0px;"/>
                          </td>
                        </tr>
                      </table>
                    </td>
                  </tr>
                  <!-- CONTENT -->
                  <tr>
                    <td style="padding: 0">
                      <div style="margin: 0px; padding: 0px;">
                        <p style="margin: 0px; padding: 0px; font-size: 13px;">
                          Estimado(a)
                          % if object.partner_id.parent_id:
                            ${object.partner_id.name} (${object.partner_id.parent_id.name}),
                          % else:
                            ${object.partner_id.name},
                          % endif
                          <br/><br/>
                          Adjunto su comprobante
                          % if object.name:
                            <strong>${object.number}</strong>
                          % else:
                            CPE
                          %endif
                          % if object.origin:
                          (con referencia: ${object.origin} )
                          % endif
                          por un importe de <strong>${object.amount_total} ${object.currency_id.name}</strong>
                          de ${object.company_id.name}.

                          % if object.state=='paid':
                              <p>Este comprobante ha sido cancelado.</p>
                          % else:
                              <p>Por favor, remita su pago tan pronto como le sea posible.</p>
                          % endif
                          <br/><br/>
                          En caso de requerir cualquier aclaración, por favor no dude en contactarnos.
                          <br/><br/>
                          Saludos,<br/>
                          % if object.user_id and object.user_id.signature:
                              ${object.user_id.signature | safe}
                          % endif
                        </p>
                      </div>
                    </td>
                  </tr>
                  <!-- FOOTER -->
                  <tr>
                    <td style="padding: 0; font-size:11px;">
                      <hr width="100%" style="background-color:rgb(204,204,204);border:medium none;clear:both;display:block;font-size:0px;min-height:1px;line-height:0; margin: 32px 0px 4px 0px;"/>
                      <b t-esc="company.name"/><br/>
                      <div style="color: #999999">
                        ${object.company_id.phone} |
                        <a href="mailto:${object.company_id.email}" style="text-decoration:none; color: #999999;">${object.company_id.email}</a>
                        % if object.company_id.website:
                          |
                          <a href="${object.company_id.website}" style="text-decoration:none; color: #999999;">
                            ${object.company_id.website}
                          </a>
                        % endif
                      </div>
                    </td>
                  </tr>
                </tbody>
              </table>
            </td>
          </tr>
          <!-- POWERED BY -->
          <tr>
            <td align="center" style="min-width: 590px; padding: 8px; font-size:11px;height:5px">

            </td>
          </tr>
        </table>
      ]]></field>
    </record>
  </data>
</odoo>
